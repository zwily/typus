<%
  page_title = t('typus.accounts.new.title')
  admin_title(page_title)
%>

<%= form_for Typus.user_class_as_symbol,
             url: { action: :create },
             html: { class: "form-signin" } do |form| %>

  <%= render partial: "form",
             locals: { form: form, submit_text: 'typus.buttons.sign_up' } %>

<% end %>
