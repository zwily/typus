<% resources.each do |key, value| %>

  <% resource = "/admin/#{key.constantize.to_resource}" %>

  <div class="resource" id="<%= key %>">

    <h2>
      <%= link_to_unless_current _(key.pluralize), :controller => resource, :action => "index" %>
    </h2>

    <% if @resource[:class].name.eql?(key) %>
      <ul>
        <% value.each do |v| %>
          <li><%= v %></li>
        <% end %>
      </ul>
    <% end %>

  </div>

<% end %>
