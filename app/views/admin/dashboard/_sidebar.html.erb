<% greeting = Typus.greetings.rand %>

<h2><%= greeting.first %>*, <%= @current_user.roles %>!</h2>

<p>Welcome to Typus. Hope you enjoy it using as much as I did developing it.</p>

<p>If you find any bug or you want to propose any feature, don't hesitate in me contact via <a href="mailto:typus@[NOSPAM]francescsesplugas.com">email</a>.</p>

<p>You can replace this file placing a file called <code>_sidebar.html.erb</code> on <code>app/views/admin/dashboard</code>.</p>

<p><small>* That's a greeting in <%= greeting.last %>.</small></p>