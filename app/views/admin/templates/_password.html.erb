<div class="clearfix" id="<%= attribute_id %>">
  <%= form.label attribute, label_text %>
  <%= form.password_field attribute, options.merge(:value => @item.send(attribute), :class => 'span12') %>
</li>
