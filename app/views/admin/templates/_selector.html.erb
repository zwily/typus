<%
  values = @resource[:class].send(attribute).map { |o| (o.kind_of?(Array) ? [ o.first, o.last ] : o) }
%>

<li>
  <%= form.label label_text %>
  <%= form.select attribute, values, options, html_options %>
</li>