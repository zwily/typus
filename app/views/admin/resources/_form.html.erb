<%= form_for @item, :url  => options[:options], :html => { :class => "resource", :multipart => true } do |form| %>

  <% if @item.errors.any? %>
  <div id="errorExplanation">
    <h3><%= pluralize(@item.errors.count, "error") %> prohibited this page from being saved:</h3>
    <ul>
    <% @item.errors.full_messages.each do |full_message| %>
      <li><%= full_message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <fieldset class="inputs">
    <ol>
      <%= raw build_form(@fields, form) %>
    </ol>
  </fieldset>

  <fieldset class="buttons">
    <ol>
      <li><%= submit_tag options[:button] , :class => "commit", :onclick => "setConfirmUnload(false);" %></li>
    </ol>
  </fieldset>

<% end %>
