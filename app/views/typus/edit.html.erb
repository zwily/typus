<% content_for :sidebar do %>
	<%= typus_block("edit_sidebar") || actions %>
<% end %>

<h2><%= link_to @model.name.pluralize.titleize, :action => 'index' %> &rsaquo; Edit</h2>

<%= typus_block("edit_top") %>

<% form_for :item, 
            :url  => { :action => "update", :id => @item.id }, 
            :html => { :multipart => true } do |f| %>
  <%= typus_form %>
  <p><%= submit_tag "Update #{@model.name.titleize}", :class => 'button' %></p>
<% end %>

<%= typus_form_has_many %>

<%= typus_form_has_and_belongs_to_many %>

<%= typus_block("edit_bottom") %>