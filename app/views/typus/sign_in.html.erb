<% form_for :typus_user, :url => { :action => 'sign_in' } do |form| %>

  <ul>

    <li>
      <%= form.label :email, _("Email") %>
      <%= form.text_field :email, :size => 20, :class => 'text' %>
    </li>

    <li>
      <%= form.label :password, _("Password") %>
      <%= form.password_field :password, :size => 20, :class => 'text' %>
    </li>

    <li>
      <%= submit_tag _("Sign in"), :class => 'button' %> <%= link_to _("Recover password"), admin_recover_password_path if Typus::Configuration.options[:recover_password] %>
    </li>

  </ul>

<% end %>