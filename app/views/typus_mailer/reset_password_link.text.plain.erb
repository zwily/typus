<%= @user.full_name %>,

You can update your password at <%= typus_recover_password_url :token => @user.token, :host => @host || 'http://test.host/' %>

If you didn't request a password update, you can ignore this message.

Have a nice day

--
<%= Typus::Configuration.options[:app_name] %>