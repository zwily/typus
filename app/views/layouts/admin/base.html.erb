<!DOCTYPE html>

<html lang="<%= I18n.locale %>">

<head>
  <%= render 'admin/shared/head' %>
</head>

<body class="<%= body_class %>">

  <div class="container-fluid">
    <div class="navbar navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <div class="brand"><%= admin_title %></div>
          <%= admin_apps %>
          <%= admin_login_info %>
        </div>
      </div>
    </div>

    <div class="row-fluid">
      <div class="span3">
        <%= build_sidebar %>
      </div>
      <div class="span9">
        <%= yield :main_grid %>
      </div>
    </div>
  </div>

</body>

</html>
